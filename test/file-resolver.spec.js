'use strict';

const expect = require('chai').expect;
const fileResolver = require('../lib');

describe('file-resolver', function() {
    it('Should throw an TypeError when dirname is not a string', function() {

    });
});